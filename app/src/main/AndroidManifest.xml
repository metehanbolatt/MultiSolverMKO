<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.deu.multisolvermko">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".createnote.CreateNoteActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="stateHidden"/>
        <activity android:name=".problemler.gezginsaticimaps.FlyHesaplama" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".problemler.gezginsaticimaps.MapsActivity"
            android:label="@string/title_activity_maps" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity android:name=".problemler.gezginsaticimaps.GsFly" />
        <activity android:name=".splashscreen.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".topsis_ahp.ahp.AhpFourProcess" />
        <activity android:name=".topsis_ahp.ahp.AhpThreeProcess" />
        <activity android:name=".topsis_ahp.topsis.TopsisFourActivity" />
        <activity android:name=".authentication.SignUpActivity" />
        <activity android:name=".authentication.SignInActivity" />
        <activity android:name=".topsis_ahp.topsis.TopsisActivity" />
        <activity android:name=".homepage.HomepageActivity" />
        <activity android:name=".information.InformationActivity" />
        <activity android:name=".problemler.gezginsaticimanuel.GezginSatici" />
        <activity android:name=".problemler.gezginsaticimaps.RoadActivity" />
        <activity android:name=".problemler.ProblemActivity" />
    </application>

</manifest>